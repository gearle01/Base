rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permite leitura pública de imagens, mas escrita apenas para administradores
    match /images/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}